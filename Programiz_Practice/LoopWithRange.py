# Loops through numbers from 1 to 5 using the `range` function (upper limit is exclusive).
for i in range(1, 6):  
    # Prints the current value of 'i' during each iteration.
    print(f"Number: {i}")

# Prints a message after all iterations are complete.
print("Loop completed!")
